import AddButton from "../src/components/Application/AddButton";

test('Check AddButton Constructor', () => {
    const obj = new AddButton();
    expect(obj).toBe(obj);
});

test('testing funcs', () => {
    let a = new AddButton({});
    a.addNewPlace
});
